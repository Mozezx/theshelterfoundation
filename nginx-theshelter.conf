# Configuração Nginx para The Shelter Foundation
# Este arquivo deve ser colocado em /etc/nginx/sites-available/theshelter.foundation
# e linkado para /etc/nginx/sites-enabled/

server {
    listen 80;
    server_name theshelter.foundation www.theshelter.foundation;

    # Logs de acesso independentes
    access_log /var/log/nginx/theshelter.access.log;
    error_log /var/log/nginx/theshelter.error.log;

    location / {
        # Aponta para o app que foi mapeado unicamente no localhost do servidor
        proxy_pass http://127.0.0.1:4100;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Repassando IP real e protocolo
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Configurações de segurança adicionais
    client_max_body_size 10M;
    proxy_read_timeout 300;
    proxy_connect_timeout 300;
    proxy_send_timeout 300;
}

# Após configurar o SSL com Certbot, este bloco será adicionado automaticamente
# server {
#     listen 443 ssl http2;
#     server_name theshelter.foundation www.theshelter.foundation;
#     
#     ssl_certificate /etc/letsencrypt/live/theshelter.foundation/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/theshelter.foundation/privkey.pem;
#     
#     # ... resto da configuração igual ao bloco acima ...
# }